# macOS
.DS_Store

# Claude Code — machine-local overrides stay out of the repo
.claude/settings.local.json

# Scratch
*.tmp

# Bicep parameter files with real governance values stay local
*.local.bicepparam
